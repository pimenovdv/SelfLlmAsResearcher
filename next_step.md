# Следующий шаг (Next Step)

**Статус:**
Добавлены функции `compute_angular_distance_between_models` и `compute_l0_distance_between_models` в `src/experiment_utils.py` с тестами. `TODO.md` обновлен.

**План действий на следующий этап:**
1. Ожидание новых задач от пользователя.
