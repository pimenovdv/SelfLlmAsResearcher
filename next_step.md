# Следующий шаг (Next Step)

**Задача:** Запуск сложной тестовой задачи на агентах и сбор результатов.

**Описание:**
Мы подготовили скрипт `run_complex_task.sh` для тестирования агентов на сложной задаче (Activation Patching с использованием `transformer_lens`). В `TODO.md` обновлены планы по дальнейшему развитию. Следующим шагом необходимо:
1. Запустить скрипт `run_complex_task.sh` с использованием реального LLM (например, `gpt-4o` или `llama3`).
2. Собрать логи выполнения и оценить работу агентов (особенно Planner и Reviewer).

**План действий на следующий этап:**
1. Выполнить скрипт `run_complex_task.sh` с настроенным `OPENAI_API_KEY`.
2. Оценить логи: понять, как Planner разбил задачу, и насколько полезным был Reviewer.
3. По результатам анализа приступить к внедрению RAG или MCP сервера, если выявится недостаток знаний у агентов (согласно `TODO.md`).
