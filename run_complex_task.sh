#!/bin/bash

# Скрипт для запуска сложной тестовой задачи для проверки агентов (Planner, Researcher, Reviewer)
# Требуется установленная переменная окружения OPENAI_API_KEY или файл .env

python3 src/agent_loop.py --planner --model "gpt-4o" --goal "Проведи эксперимент по Activation Patching на модели 'gpt2-small' с использованием библиотеки 'transformer_lens'. Задача: выяснить, какие MLP слои вносят наибольший вклад в предсказание конкретного токена (например, извлечение конкретного типа знаний при подмене субъекта в предложении 'The Eiffel Tower is in'). Напиши полные Python-скрипты для каждого шага эксперимента, не выводи сырые веса."
