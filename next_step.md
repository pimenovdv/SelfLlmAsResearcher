# Следующий шаг (Next Step)

**Статус:**
Интеграция новых метрик (top_k_accuracy, MRR) в Factual Recall завершена.

**План действий на следующий этап:**
1. Вынести повторяющийся код из скриптов экспериментов (загрузка модели, базовая обработка) в общие утилиты `src/experiment_utils.py`.
